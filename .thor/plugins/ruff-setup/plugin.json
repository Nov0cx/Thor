{
    "name": "ruff Setup",
    "description": "Checks whether ruff is on PATH and turns installing it into a workspace task, since installing usually runs longer than a plugin call is allowed to block for. This workspace's .thor/lsp.json uses ruff as the .py/.pyi server (formatting; basedpyright is disabled here).",
    "permissions": ["exec", "read", "write", "ui"]
}
