{
    "name": "zls Setup",
    "description": "Checks whether zls is on PATH and turns installing it into a workspace task, since installing usually runs longer than a plugin call is allowed to block for.",
    "permissions": ["exec", "read", "write", "ui"]
}
